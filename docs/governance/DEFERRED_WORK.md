# Deferred Work Register

Rule 12 / Rule 11. This register survives the session. Future agents resume from here.

## Format
- `[DATE] <scope>: <what> — <why deferred> — <resume hint> — <status>`

## Items
- [2026-08-27] Windows desktop release: Package a self-contained Windows service installation path — resolved in the current branch with `RemoteCTRLServer.exe` and native PowerShell boot-startup scripts; install/start/uninstall still requires clean-machine validation — in verification
- [2026-08-27] Windows desktop service configuration: Normalize service installer configuration keys — resolved in the current branch through a shared `AppConfig` environment mapper and automated mapping test — in verification
- [2026-08-27] Windows desktop verification: Add tray, installer, and release-archive smoke coverage — build outputs and required release contents are now asserted in CI; install/start/uninstall still requires clean-machine validation — in verification
- [2026-08-27] Windows application control: Provide a trusted Authenticode certificate and publisher allow-list for managed devices — signing automation is now present and verified end-to-end locally with a free self-signed dev cert (`scripts/create-dev-signing-cert.ps1`); a publicly-trusted CA certificate still needs to be purchased and its PFX added to CI secrets before a real signed release can ship — blocked on certificate purchase / budget owner
- [2026-08-27] Tagged release CI will currently fail: `windows-release` in `ci.yml` always calls `sign-windows.ps1 -RequireSignature`, which throws if `WINDOWS_SIGNING_PFX_BASE64`/`WINDOWS_SIGNING_PFX_PASSWORD` secrets are unset — pushing a `v*` tag today produces no release artifact, not an unsigned fallback — resolve by either configuring the real signing secrets (see above) or making the release job fail loudly with a clear message instead of the current generic signtool error — blocked on certificate purchase, same root cause as the item above
- [2026-08-27] Clean-machine / real end-user install validation: everything so far has been verified on one dev workstation (author's own machine, already has Node/pnpm/OpenSSH/PowerShell 7 present) — install/uninstall, boot-startup, and SmartScreen/Application-Control behavior have not been verified on a genuinely clean, non-developer Windows machine — in verification
- [2026-08-27] Cloudflare quick-tunnel URL is ephemeral: resolved by adding optional named-tunnel support (`CLOUDFLARE_TUNNEL_TOKEN` + `CLOUDFLARE_TUNNEL_HOSTNAME`, see README "Persistent tunnel"); requires the user to create a tunnel in the Cloudflare Zero Trust dashboard, which itself requires a domain in their Cloudflare account (not tested against a real Cloudflare account/domain this session — the code path is covered by unit tests but not an actual named-tunnel connection) — in verification
- [2026-08-27] Push-based "your server URL changed" notice to already-registered devices — not started; only relevant if a user chooses to stay on the ephemeral quick tunnel rather than setting up a named tunnel
- [2026-08-27] iOS app has not been built/run on a device or simulator this session — SSH key-auth pairing was verified server-side (raw `ssh -i` test) and the Connection screen's expected inputs were confirmed by reading the source, but the actual app UI flow (paste key, tap Test Connection, use Terminal/Files/Processes tabs) has not been exercised end-to-end — in verification
